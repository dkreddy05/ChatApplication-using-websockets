1. Users Table:
Schema:
user_id (PK), full_name, email, phone_number, date_of_birth, address

Primary Key: user_id

1NF:

All fields are atomic (no multivalued attributes, no repeating groups).

2NF:

All non-key attributes (full_name, email, phone_number, date_of_birth, address) depend fully on user_id.

3NF:

No transitive dependencies; all non-key attributes are directly dependent on user_id.

✅ Users table is in 3NF.

2. Accounts Table:
Schema:
account_id (PK), user_id (FK), account_number, account_type, balance, created_at

Primary Key: account_id

1NF:

All attributes are atomic and have single values.

2NF:

All non-key attributes (user_id, account_number, account_type, balance, created_at) depend fully on account_id.

3NF:

No transitive dependencies; all non-key attributes depend directly on account_id.

✅ Accounts table is in 3NF.

3. Transactions Table:
Schema:
transaction_id (PK), account_id (FK), transaction_type, amount, transaction_time, status, location, device_info

Primary Key: transaction_id

1NF:

All fields are atomic, no repeating groups.

2NF:

All non-key attributes depend completely on transaction_id.

3NF:

No transitive dependencies exist.

✅ Transactions table is in 3NF.

4. Suspicious_Transactions Table:
Schema:
suspicious_id (PK), transaction_id (FK), reason, detected_on, is_fraud

Primary Key: suspicious_id

1NF:

All attributes are atomic and contain single values.

2NF:

All non-key attributes (transaction_id, reason, detected_on, is_fraud) depend fully on suspicious_id.

3NF:

No transitive dependencies are present.

✅ Suspicious_Transactions table is in 3NF.

5. Fraud_Reports Table:
Schema:
report_id (PK), suspicious_id (FK), investigator_name, report_date, conclusion, action_taken

Primary Key: report_id

1NF:

All attributes are atomic and have single values.

2NF:

All non-key attributes (suspicious_id, investigator_name, report_date, conclusion, action_taken) depend fully on report_id.

3NF:

No transitive dependencies.

✅ Fraud_Reports table is in 3NF.

6. Login_Activity Table:
Schema:
login_id (PK), user_id (FK), login_time, ip_address, device_info, location

Primary Key: login_id

1NF:

All fields are atomic and have single values.

2NF:

All non-key attributes depend fully on login_id.

3NF:

No transitive dependencies exist.

✅ Login_Activity table is in 3NF.

